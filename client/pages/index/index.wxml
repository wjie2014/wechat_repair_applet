<!--index.wxml-->
<view class="container">
  <!-- <form bindsubmit="submitInfo" report-submit='true' >
  <button form-type="submit" type="default" size="mini">提交</button>
</form> -->
  <!-- 用户登录测试 -->
  <view class="userinfo">
    <view class='userinfo-avatar'>
      <open-data type="userAvatarUrl"></open-data>
    </view>
    <!-- <image
            class="userinfo-avatar"
            src="{{logged ? userInfo.avatarUrl : './user-unlogin.png'}}"
            background-size="cover"
        ></image> -->
    <view>
      <!-- <text class="userinfo-nickname">{{logged ? userInfo.nickName : '点击登录'}}</text> -->
      <block wx:if="{{roleType==1}}">

        <block wx:if="{{name}}">
          <text class="userinfo-nickname">{{name}}-{{roleName}}</text>
        </block>
        <block wx:else>
          <open-data class='userinfo-nickname' type="userNickName"></open-data>
        </block>
      </block>
      <block wx:else>
        <block wx:if="{{studentInfo}}">
          <text class="userinfo-nickname">{{name}}-{{relationship}}</text>
        </block>
        <block wx:else>
          <open-data class='userinfo-nickname' type="userNickName"></open-data>
        </block>
      </block>

    </view>
  </view>
  <view class="tunnel">
    <!-- <view class="tunnel-text">
      <navigator url="../contactIndex/contactIndex" open-type="navigate" class="uploader-text">
        <text>通讯录</text>
      </navigator>
    </view> -->
    <view class="tunnel-text">
      <text class="uploader-text" bindtap='auth'>身份认证</text>
    </view>
    <!-- <view class="tunnel-text" wx:if="{{userInfo.type==2}}">
      <navigator url="../userInfo/userInfo" open-type="navigate" class="uploader-text">
        <text>维修人员管理</text>
      </navigator>
    </view> -->
  </view>
  <!-- 任务管理 -->
  <view wx:if="{{userInfo.type!=0}}" style='width:100%;color:#444444;padding:10rpx;font-size:27rpx;'> </view>
    <view class="tunnel" wx:if="{{userInfo.type==1}}">
    <view class="tunnel-text" bindtap='goList4'>
        <text class='item-view'>待处理工单</text>
    </view>
    <view class="tunnel-text" bindtap='goList5'>
        <text class='item-view'>已完成工单</text>
    </view>
  </view>
  <view class="tunnel" style='margin-top:0px;' wx:if="{{userInfo.type==2}}">
    <view class="tunnel-text" bindtap='goList1'>
        <text class='item-view'>待分配工单</text>
    </view>
    <view class="tunnel-text" bindtap='goList2'>
        <text class='item-view'>已分配工单</text>
    </view>
    <view class="tunnel-text" bindtap='goList3'>
        <text class='item-view'>已完成工单</text>
    </view>
  </view>
  <!-- 信道连接测试 -->
  <!-- <view class="tunnel">
        <view class="tunnel-text uploader-text">
            <text bindtap="doRequest">请求（是否带有登陆态）</text>
            <switch class="tunnel-switch" bindchange="switchRequestMode" color="#007AFF"/>
        </view>
        <view class="tunnel-text" wx:if="{{requestResult}}">
            <text class="request-text">{{'请求结果：' + requestResult}}</text>
        </view>
    </view> -->



  <!-- 上传图片测试 -->
  <!-- <view class="uploader">
        <view class="uploader-text" bindtap="doUpload">
            <text>上传图片</text>
        </view>
        <view class="uploader-container" wx:if="{{imgUrl}}">
            <image
                class="uploader-image"
                src="{{imgUrl}}"
                mode="aspectFit"
                bindtap="previewImg"
            ></image>
        </view>
    </view> -->

  <!-- 信道连接测试 -->
  <!-- <view class="tunnel">
        <view class="tunnel-text">
            <text>信道</text>
            <switch class="tunnel-switch" bindchange="switchChange" color="#007AFF"/>
        </view>
        <view class="tunnel-text {{ tunnelStatus && tunnelStatus === 'connected' ? 'active' : 'disable' }}" bindtap="sendMessage">
            <text>发送消息</text>
        </view>
    </view> -->

  <!-- 上传图片测试 -->
  <!-- <view class="uploader">
        <navigator url="../addCgi/addCgi" open-type="navigate" class="uploader-text">
            <text>快速增加 CGI</text>
        </navigator>
    </view> -->
  <view style='bottom:0;color:#aaaaaa;position:fixed;padding:10rpx;font-size:24rpx;'>技术支持：18920609602(微信同号)</view>
</view>